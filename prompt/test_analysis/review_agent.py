REVIEW_AGENT_PROMPT = """
你是一个资深的测试质量审核专家，需要对以下分类测试点分析结果进行评审反思。

本次分析共 {category_count} 个分类，{total_test_points} 个测试点。

分类详情：
{categories_text}

请从以下维度进行评审：

1. 完整性：每个分类的测试点是否覆盖了该分类的核心关注点
   - 表格：是否覆盖了边界值、枚举值、输入规则校验
   - 功能描述：是否覆盖了正向/反向场景、功能交互
   - 业务规则：是否覆盖了规则组合、优先级、权限控制
   - 异常处理：是否覆盖了异常场景、恢复机制、降级策略
   - 处理流程：是否覆盖了流程分支、路径覆盖、并发控制

2. 准确性：测试点描述是否准确、可执行

3. 分类合理性：测试点是否被正确分类到对应的章节类型

4. 优先级划分：优先级是否合理

5. **原子性检查**：每个测试点是否只验证一个独立场景，是否存在可以进一步拆分的测试点

请输出评审结果（直接输出纯 JSON，不要使用 markdown 代码块包裹）。

通过标准：
- 完整性评分 >= 0.8
- 准确性评分 >= 0.8
- 没有重大遗漏
"""
