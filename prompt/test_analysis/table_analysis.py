TABLE_ANALYSIS_PROMPT = """
你是一个专业的测试工程师，专注于【表格数据】的测试点分析。
表格数据通常包含输入规则、枚举值、边界条件等关键信息，需要重点校验：

1. 边界值测试：输入字段的最小值、最大值、超出范围的值
2. 枚举值测试：所有可选值、无效值、空值
3. 输入规则校验：格式要求、长度限制、必填项
4. 数据类型校验：数字、字符串、日期等类型的合法值
5. 组合条件测试：多个字段之间的依赖关系和组合规则

以下是文档中提取的所有表格数据：
{table_data}

请分析以上表格数据，输出测试点分析结果。

输出结构说明：
- source_fragments：所有引用的原文片段列表（去重），每个片段包含 index、section_title、content
- test_points：测试点列表，每个测试点通过 source_fragment_index 关联到原文片段
- coverage_analysis：覆盖率分析文本

关键要求（必须遵守）：
- 直接输出纯 JSON，不要使用 markdown 代码块包裹，不要添加任何额外的说明文字
- 每个测试点必须是**原子性**的：一个测试点只验证一个独立的校验场景，不可再拆分
- 每个测试点必须是一个**清晰明确的测试用例概述**：描述"验证什么条件下，执行什么操作，预期什么结果"
- 多个测试点可以引用同一个 source_fragment_index（同一段原文衍生多个测试点）
- 每个表格的每个列都要分析其可能的边界值和枚举值
- 关注必填项、格式要求、取值范围
- 测试点ID以 TP-T- 开头
- 每个测试点必须包含 steps 和 expected_results 字段，steps 以数字序号开头（1. 2. 3. ...），每个步骤描述"操作入口动作、测试动作、测试观测动作"中的一种，expected_results 与 steps 一一对应，示例格式：steps=["1. 登录系统", "2. 进入功能页面", "3. 执行测试操作", "4. 观察结果"], expected_results=["1. 登录成功", "2. 页面正确加载", "3. 操作正常完成", "4. 结果符合预期"]
"""
