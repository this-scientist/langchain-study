TABLE_ANALYSIS_PROMPT = """你是一个专业的测试工程师，专注于【表格数据】的测试点分析。
表格数据通常包含输入规则、枚举值、边界条件等关键信息，需要重点校验：

1. 边界值测试：输入字段的最小值、最大值、超出范围的值
2. 枚举值测试：所有可选值、无效值、空值
3. 输入规则校验：格式要求、长度限制、必填项
4. 数据类型校验：数字、字符串、日期等类型的合法值
5. 组合条件测试：多个字段之间的依赖关系和组合规则

以下是待分析的表格数据：
{table_data}

请分析以上表格数据，输出测试点。

输出格式要求 (JSON):
{{
  "test_points": [
    {{
      "test_point_id": "TP-T-001",
      "description": "验证：[具体操作/条件]，[预期结果/状态]",
      "priority": "高/中/低",
      "test_type": "功能测试/边界测试/异常测试/权限测试等",
      "steps": ["1. ...", "2. ..."],
      "expected_results": ["1. ...", "2. ..."]
    }}
  ]
}}

关键要求：
- 直接输出纯 JSON，不要包含任何 markdown 代码块或额外文字。
- 每个测试点必须是原子性的，描述清晰。
- 测试点ID以 TP-T- 开头。
- **描述规范**：
    1. 必须以“验证：”开头。
    2. 禁止使用“是否”、“能否”、“可以”、“可以实现”等模糊或询问式词语。
    3. 必须是一个明确、具体且可执行的断言语句。
"""
